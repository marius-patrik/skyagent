# Handoff

Do not restore deleted provider runtime or distribution artifacts. If an interface needs model reasoning, Agent OS invokes SkyAgent tools from its canonical session; SkyAgent itself stays deterministic and provider-neutral.

Any future state must be a documented SkyBlock domain record beneath `$AGENTS_HOME/runtime/apps/skyagent`. Any future secret must be registered in Agent OS and referenced by manifest scope. Add one canonical behavior name only; do not retain prior names as aliases.

Run every command in `COMMANDS.md` before changing this status to complete.
