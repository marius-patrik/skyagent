{
  "name": "skyagent",
  "version": "0.1.0",
  "description": "Codex plugin, CLI, and MCP tools for Hypixel SkyBlock profile analysis.",
  "type": "module",
  "bin": {
    "skyagent": "./scripts/skyagent.mjs"
  },
  "scripts": {
    "check": "node --check scripts/skyagent.mjs && node --check scripts/mcp-server.mjs && node --check scripts/lib/hypixel.mjs && node --check scripts/lib/store.mjs && node --check scripts/lib/profile.mjs"
  },
  "license": "MIT"
}
